#include <stdio.h>
#include <math.h>

/*Create a program that computes the result of
a number raised to the power of the square root of a number.

1. Take  input from user
2. Compute the square root of a number using sqrt()
3. Compute the power of the number raised to the power of its sqare root
4. Print the result

*/
int input()
{
    // int num = 25;
    // // int num2;

    // printf("Enter first Number:  ");
    // scanf("%d", &num);

    // // printf("Enter second Number:  ");
    // // scanf("%d", &num2);

    // double total = sqrt(num);
    // printf("%lf", sqrt(num));

    // int totalsqrt = sqrt(num);
    // printf("%d", totalsqrt);
    // int totalpow = pow(totalsqrt, totalsqrt);

    // printf("%d", totalpow);
}

int main()
{
    int num = 25;
    printf("%lf", sqrt(num));
    // input();
    return 0;
}